import { Card, CardContent } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { Progress } from "@/components/ui/progress";
import { Tabs, TabsList, TabsTrigger, TabsContent } from "@/components/ui/tabs";
import { ScrollArea } from "@/components/ui/scroll-area";
import { Gift, Star, Clock } from "lucide-react";

export default function LoyaltyApp() {
  return (
    <div className="p-4 font-['Roboto'] bg-white text-[#07685f] min-h-screen">
      <h1 className="text-2xl font-bold mb-4">Loyalty Program</h1>

      <Card className="bg-[#07685f] text-white rounded-2xl shadow-md mb-4">
        <CardContent className="p-4">
          <div className="text-lg">You have</div>
          <div className="text-3xl font-bold">1,200 Points</div>
          <div className="mt-2 text-sm">Gold Tier - 800 pts to Platinum</div>
          <Progress value={60} className="bg-white mt-2" />
        </CardContent>
      </Card>

      <Button className="w-full bg-[#07685f] text-white rounded-xl mb-4">Redeem Points</Button>

      <h2 className="text-xl font-semibold mb-2">Rewards</h2>
      <ScrollArea className="mb-4 whitespace-nowrap">
        <div className="flex space-x-3">
          {["Coffee Coupon", "Discount 10%", "Gift Card"].map((reward, idx) => (
            <Card key={idx} className="min-w-[120px] p-3 flex items-center flex-col text-center shadow-sm">
              <Gift className="mb-2" />
              <span className="text-sm">{reward}</span>
            </Card>
          ))}
        </div>
      </ScrollArea>

      <Tabs defaultValue="history">
        <TabsList className="bg-[#e0f2f1]">
          <TabsTrigger value="history" className="text-[#07685f]">History</TabsTrigger>
          <TabsTrigger value="info" className="text-[#07685f]">How It Works</TabsTrigger>
        </TabsList>

        <TabsContent value="history">
          <ul className="mt-2 space-y-2 text-sm">
            <li className="flex justify-between"><span>* Earned 200 pts</span><span>June 15</span></li>
            <li className="flex justify-between"><span>* Redeemed Coffee</span><span>June 10</span></li>
            <li className="flex justify-between"><span>* Earned 300 pts</span><span>June 1</span></li>
          </ul>
        </TabsContent>

        <TabsContent value="info">
          <div className="mt-2 text-sm">
            Earn points by using your Bogd Bank card. Redeem for gifts, discounts, and experiences.
            <br />
            Tiers: Bronze (0-500), Silver (500-1000), Gold (1000-2000), Platinum (2000+)
          </div>
        </TabsContent>
      </Tabs>
    </div>
  );
}
